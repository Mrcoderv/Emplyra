'use client'
import { useEffect, useState } from 'react'
import { CalendarDays, Clock3, ShieldCheck, Users } from 'lucide-react'
import { api, type DashboardSummary } from '@/lib/api'
const cards=[['total_employees','Total employees',Users],['active_employees','Active employees',ShieldCheck],['employees_on_leave','On leave today',CalendarDays],['pending_leave_requests','Pending approvals',Clock3]] as const
export default function DashboardPage(){const [data,setData]=useState<DashboardSummary|null>(null);const [error,setError]=useState('');useEffect(()=>{api.dashboard().then(setData).catch(e=>setError(e instanceof Error?e.message:'Unable to load dashboard'))},[]);return <section><div className="mb-8 flex flex-col gap-2"><p className="text-sm font-medium text-primary">Overview</p><h1 className="text-3xl font-semibold tracking-tight">Good morning{data?'':' — welcome back'}</h1><p className="text-sm text-muted-foreground">Here is what is happening across your organization today.</p></div>{error&&<div role="alert" className="mb-6 rounded-xl border border-destructive/30 bg-destructive/10 p-4 text-sm text-destructive">{error}</div>}<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([key,label,Icon])=><div key={key} className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center justify-between"><p className="text-sm text-muted-foreground">{label}</p><Icon className="text-primary" /></div><p className="mt-6 text-3xl font-semibold">{data?.[key] ?? '—'}</p><p className="mt-2 text-xs text-muted-foreground">Live from your HRMS API</p></div>)}</div><div className="mt-6 rounded-2xl border border-border bg-card p-6"><h2 className="font-semibold">Your operating rhythm</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Use the navigation to move from organization-wide signals into employee records, time off, payroll, and people programs. Access is scoped to your role.</p></div></section>}
