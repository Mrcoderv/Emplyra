'use client'
import Link from 'next/link'
import { Search } from 'lucide-react'
import { useEffect, useState } from 'react'
import { api, type Employee } from '@/lib/api'
export default function EmployeesPage(){const [rows,setRows]=useState<Employee[]>([]);const [query,setQuery]=useState('');const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{setLoading(true);api.employees(1,20,query).then(r=>setRows(r.items||[])).catch(e=>setError(e instanceof Error?e.message:'Unable to load employees')).finally(()=>setLoading(false))},[query]);return <section><div className="mb-8 flex flex-col gap-2"><p className="text-sm font-medium text-primary">Directory</p><h1 className="text-3xl font-semibold tracking-tight">Employees</h1><p className="text-sm text-muted-foreground">Find people, roles, and reporting context.</p></div><div className="rounded-2xl border border-border bg-card"><div className="border-b border-border p-5"><div className="relative max-w-sm"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search employees" className="h-10 w-full rounded-xl border border-input bg-background pl-10 pr-3 text-sm outline-none focus:ring-2 focus:ring-ring"/></div></div>{error?<p role="alert" className="p-8 text-sm text-destructive">{error}</p>:loading?<p className="p-8 text-sm text-muted-foreground">Loading employee records…</p>:rows.length?<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-muted/50 text-xs uppercase text-muted-foreground"><tr><th className="px-5 py-3">Employee</th><th className="px-5 py-3">Department</th><th className="px-5 py-3">Status</th></tr></thead><tbody>{rows.map(e=><tr key={e.id} className="border-t border-border hover:bg-muted/30"><td className="px-5 py-4"><Link className="font-medium hover:text-primary" href={`/employees/${e.id}`}>{e.first_name} {e.last_name}</Link><p className="text-xs text-muted-foreground">{e.email||e.employee_code||'Employee record'}</p></td><td className="px-5 py-4 text-muted-foreground">{typeof e.department==='string'?e.department:e.department?.name||'Unassigned'}</td><td className="px-5 py-4">{e.status||e.employment_status||'Active'}</td></tr>)}</tbody></table></div>:<p className="p-12 text-center text-sm text-muted-foreground">No employees found.</p>}</div></section>}
