'use client'
import { FormEvent, useState } from 'react'
import { ArrowRight, Eye, EyeOff, ShieldCheck } from 'lucide-react'
import { useAuth } from '@/lib/auth'
export default function LoginForm() { const { login } = useAuth(); const [identifier,setIdentifier]=useState(''); const [password,setPassword]=useState(''); const [show,setShow]=useState(false); const [error,setError]=useState(''); const [pending,setPending]=useState(false); async function submit(e: FormEvent) { e.preventDefault(); if (e.nativeEvent instanceof SubmitEvent && (e as unknown as KeyboardEvent).isComposing) return; setPending(true); setError(''); try { await login(identifier,password); window.location.replace('/dashboard') } catch (err) { setError(err instanceof Error ? err.message : 'Unable to sign in') } finally { setPending(false) } } return <main className="grid min-h-screen bg-background lg:grid-cols-[1.05fr_0.95fr]"><section className="hidden bg-primary p-12 text-primary-foreground lg:flex lg:flex-col lg:justify-between"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-primary-foreground text-primary font-bold">e</span><span className="text-lg font-semibold">Emplyra</span></div><div className="max-w-lg"><p className="mb-5 text-sm font-medium uppercase tracking-[0.2em] opacity-70">People operations, clarified</p><h1 className="text-5xl font-semibold leading-tight tracking-tight">Make every people decision feel lighter.</h1><p className="mt-6 max-w-md text-base leading-7 opacity-75">A focused workspace for the teams, moments, and decisions that move your organization forward.</p></div><p className="text-sm opacity-60">Secure workspace access</p></section><section className="flex items-center justify-center p-6 sm:p-12"><div className="w-full max-w-md"><div className="mb-10 lg:hidden"><span className="grid size-10 place-items-center rounded-xl bg-primary text-primary-foreground font-bold">e</span></div><p className="mb-3 text-sm font-medium text-primary">Welcome back</p><h2 className="text-3xl font-semibold tracking-tight">Sign in to Emplyra</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Use your email or username to access your workspace.</p><form onSubmit={submit} className="mt-8 flex flex-col gap-5"><label className="flex flex-col gap-2 text-sm font-medium">Email or username<input required value={identifier} onChange={e=>setIdentifier(e.target.value)} className="h-11 rounded-xl border border-input bg-background px-3 outline-none ring-offset-background focus:ring-2 focus:ring-ring" autoComplete="username" /></label><label className="flex flex-col gap-2 text-sm font-medium">Password<div className="relative"><input required type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} className="h-11 w-full rounded-xl border border-input bg-background px-3 pr-11 outline-none focus:ring-2 focus:ring-ring" autoComplete="current-password" /><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground" aria-label={show?'Hide password':'Show password'}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<p role="alert" className="text-sm text-destructive">{error}</p>}<button disabled={pending} className="inline-flex h-11 items-center justify-center gap-2 rounded-xl bg-primary px-4 text-sm font-semibold text-primary-foreground disabled:opacity-50">{pending?'Signing in…':'Continue'}<ArrowRight /></button></form><p className="mt-8 flex items-center gap-2 text-xs leading-5 text-muted-foreground"><ShieldCheck /> Your session is protected with JWT and permission-scoped access.</p></div></section></main> }
