import { PermissionGate } from '@/lib/guards'
import type { NavItem } from '@/lib/navigation'
export default function ModulePlaceholder({ item }: { item: NavItem }) { const Icon=item.icon; return <section className="flex min-h-[520px] flex-col"><div className="mb-8 flex flex-col gap-2"><p className="text-sm font-medium text-primary">{item.href}</p><h1 className="text-3xl font-semibold tracking-tight">{item.label}</h1><p className="text-sm text-muted-foreground">{item.description}</p></div><PermissionGate permission={item.permission} fallback={<div className="rounded-2xl border border-border bg-card p-8 text-center"><h2 className="font-semibold">Permission required</h2><p className="mt-2 text-sm text-muted-foreground">Your role does not have access to this module.</p></div>}><div className="flex min-h-80 flex-col items-center justify-center gap-3 rounded-2xl border border-dashed border-border bg-card p-8 text-center"><div className="grid size-12 place-items-center rounded-2xl bg-primary/10 text-primary"><Icon /></div><h2 className="text-lg font-semibold">{item.label} workspace</h2><p className="max-w-md text-sm leading-6 text-muted-foreground">This workspace is connected to the verified API contract. Live records and mutations will appear here as the module is implemented.</p></div></PermissionGate></section> }
